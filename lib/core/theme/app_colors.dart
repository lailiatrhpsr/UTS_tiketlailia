import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Netral / permukaan
  static const ink = Color(0xFF12171C);
  static const inkMuted = Color(0xFF5B6572);
  static const paper = Color(0xFFF6F4EF);
  static const surface = Color(0xFFFFFFFF);
  static const line = Color(0xFFE4E0D6);

  // Dark mode
  static const inkDarkBg = Color(0xFF12151A);
  static const inkDarkSurface = Color(0xFF1B1F26);
  static const inkDarkLine = Color(0xFF2A2F38);

  // Brand
  static const brand = Color(0xFF2196F3);
  static const brandDeep = Color(0xFF1565C0);
  static const brandTint = Color(0xFFE3F2FD);

  static const statusOpen = Color(0xFFC6742F);
  static const statusAssigned = Color(0xFF6E5DA6);
  static const statusInProgress = Color(0xFF1E6FD9);
  static const statusClosed = brand;
}